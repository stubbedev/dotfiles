#!/usr/bin/env zsh

SELECTED_DIR="$(find . -maxdepth 5 -mindepth 1 -type d ! -path '*/\.*' -print 2>/dev/null | fzf --algo=v1 --query="$*")"

if [[ -n "$SELECTED_DIR" ]]; then
  echo "$SELECTED_DIR"
else
  echo ""
fi

