#!/usr/bin/env zsh

if command -v btop &>/dev/null; then
  monitor=btop
elif command -v htop &>/dev/null; then
  monitor=htop
else
  exit 0
fi

"$monitor"
[[ -n "$TMUX" ]] && { tmux kill-pane || exit 0; }
