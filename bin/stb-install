#!/bin/sh

info() {
  echo "WELCOME TO THE STUBBEDEV DOTFILES INSTALLER"
  mkdir -p "$HOME/.ssh"
  SCRIPT_DIR="$PWD"
  export SCRIPT_DIR
  . "$SCRIPT_DIR/bin/install-funcs"
  installer_prompt
}

info
