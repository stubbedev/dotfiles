#!/usr/bin/env zsh

if ! command -v claude &>/dev/null; then
  exit 0
fi

if [[ -z "$TMUX" ]]; then
  claude --dangerously-skip-permissions "$@"
else
  tmux renamew "claude"
  claude --dangerously-skip-permissions "$@"
  tmux kill-pane || exit 0
fi
