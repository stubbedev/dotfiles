# Prevent initial audio pop when starting playback
# This happens when streams start before buffers are adequately filled

stream.properties = {
    # Skip channel remix when source/sink layouts already match
    stream.dont-remix = true
}
