set imap_user = "alexander.bugge.stage@gmail.com"
set imap_pass = "`gpg --quiet --batch --decrypt ~/.config/neomutt/accounts/gmail/alexander.bugge.stage/password.gpg`"
set smtp_url = "smtp://$imap_user@smtp.gmail.com:587/"
set smtp_pass = "`gpg --quiet --batch --decrypt ~/.config/neomutt/accounts/gmail/alexander.bugge.stage/password.gpg`"


set from = "alexander.bugge.stage@gmail.com"
set realname = "Alexander Stage"

set folder = "imaps://$imap_user@imap.gmail.com:993"
set spoolfile = "+INBOX"
set postponed = "+[Gmail]/Drafts"
set record = "+[Gmail]/Sent"
set trash = "+[Gmail]/Trash"

# Be sure to `mkdir -p` the following paths
set header_cache =~/.config/neomutt/accounts/gmail/alexander.bugge.stage/cache/headers
set message_cachedir =~/.config/neomutt/accounts/gmail/alexander.bugge.stage/cache/bodies
set certificate_file =~/.config/neomutt/accounts/gmail/alexander.bugge.stage/cert

set move = no
set imap_keepalive = 900

set sort = reverse-threads
set sort_aux = last-date-received
