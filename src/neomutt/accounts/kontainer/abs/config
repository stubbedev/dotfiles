set imap_user = "abs@kontainer.com"
set imap_pass = "`gpg --quiet --batch --decrypt ~/.config/neomutt/accounts/kontainer/abs/password.gpg`"
set smtp_url = "smtps://abs@kontainer.com@ex.konformit.com:587/"
set smtp_pass = "`gpg --quiet --batch --decrypt ~/.config/neomutt/accounts/kontainer/abs/password.gpg`"

set from = "abs@kontainer.com"
set realname = "Alexander Stage"

set folder = "imaps://$imap_user@ex.konformit.com:993"
set spoolfile = "+INBOX"
set postponed = "+Drafts"
set record = "+Sent"
set trash = "+Trash"

# Be sure to `mkdir -p` the following paths
set header_cache = ~/.config/neomutt/accounts/kontainer/abs/cache/headers
set message_cachedir = ~/.config/neomutt/accounts/kontainer/abs/cache/bodies
set certificate_file = ~/.config/neomutt/accounts/kontainer/abs/cert

set move = no
set imap_keepalive = 900

set sort = reverse-threads
set sort_aux = last-date-received

