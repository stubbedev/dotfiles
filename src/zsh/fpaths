#!/usr/bin/env zsh

# zsh evaluates fpath at compinit time but ignores entries that don't
# resolve to an existing directory, so listing both candidate profile
# locations costs nothing on the platform that doesn't have one.
function _fpaths_init {
  local dir="$HOME/.stubbe/src/zsh/fpaths.d"
  [[ -d "$dir" ]] || mkdir -p "$dir"
  local default_dir="$HOME/.stubbe/src/zsh/fpaths.default.d"
  [[ -d "$default_dir" ]] || mkdir -p "$default_dir"
  fpath+=(
    "$default_dir"
    "$dir"
    "$HOME/.nix-profile/share/zsh/site-functions"
    "/etc/profiles/per-user/$USER/share/zsh/site-functions"
  )
}
_fpaths_init
unfunction _fpaths_init
